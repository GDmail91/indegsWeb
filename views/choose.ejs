<% include header.ejs %>
<div class="page-header"><h1><%= title %><small><%= card.title %></small></h1></div>
<div class="row">
    <div class="col-md-6 col-sm-6">
        <img src="<%= imageA.url %>" class="img-responsive" alt="imageA">
    </div>
    <div class="col-md-6 col-sm-6">
        <img src="<%= card.imageB %>" class="img-responsive" alt="imageB">
        <p><%= card.title %>
            <%= card %></p>
    </div>

</div>

<% include footer.ejs %>